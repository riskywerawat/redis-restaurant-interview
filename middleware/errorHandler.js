module.exports = ( err , req, res) => {
    // const statusCode = err.statusCode || 500 ;
    // return res.status(err.statusCode).json({
    //     error:{
    //         statu_code : statusCode,
    //         message : err.message,
    //         validation: err.validation
    //     }
    // })
}